{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Contact creation</h1>

    {{ form(form) }}


        <a class="btn btn-info" href="{{ path('contact') }}">
            Back to the list
        </a>

{% endblock %}
